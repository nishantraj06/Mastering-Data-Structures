#Two-Pointer Pattern

#Core Concept:
 Use two indices (usually left and right) to traverse an array/string.

#Why? It saves time by avoiding unnecessary repetitions.

#Time Complexity: O(N)
#Space Complexity: O(1) 

(since we only use two variables for pointers).